# ES2021 Modern Usage

1. **Use replaceAll()** instead of regex or split/join
2. **Use `??=`** for lazy initialization
3. **Use numeric separators** for large numbers
4. **Use Promise.any()** for fastest response

All features: Chrome 85+, Firefox 79+, Safari 14+, Node.js 15+
