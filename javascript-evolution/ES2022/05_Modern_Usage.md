# ES2022 Modern Usage

1. **Use private fields** (`#`) for true encapsulation
2. **Use top-level await** in modules
3. **Use .at()** for negative indexing
4. **Use Object.hasOwn()** instead of hasOwnProperty

Support: Chrome 94+, Firefox 90+, Safari 15+, Node.js 16.9+
